apiVersion: v1
kind: Service
metadata:
  name: sonarqube
  namespace: {{ .Release.Name }}
  labels:
    app: sonarqube
spec:
  type: NodePort
  ports:
  - name: sonarqube
    protocol: TCP
    port: {{ .Values.sonarqube.port }}
  selector:
    app: sonarqube