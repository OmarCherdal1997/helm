{{- $relname := .Release.Name -}}

{{ with .Values.sonarqube }}
{{ if eq .perstisted true }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: sonaqube-data
spec:
  accessModes:
  - {{ .storage_access_mode }}
  capacity:
    storage: {{ .data_storage_size }}
  nfs:
    path: /nfsroot/sonarqube_data
    server: {{ .persistance_sever }}

{{ end }}

{{ end }}